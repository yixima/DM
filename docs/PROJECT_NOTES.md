# 参照情報記録（本案件の確定事項・未決事項）

> 汎用マニュアルには書かない、この案件固有の情報をここに集約する。
> 新しいチャットへ移るときは、このファイルと `README.md` を読めば状況を再現できる状態を保つ。
>
> 最終更新: 2026-08-27

---

## 1. この案件で作っているもの

`data/master_contacts_20260825_181226.csv` を元に、

1. 送信先へ一括・定期的にメールで案内を送る
2. 各社の問い合わせフォームへ自動・一斉・定期的に案内を送る

を、同じ宛先リスト・同じ配信ルール・同じ履歴の上で回すシステム。

- リポジトリ: `yixima/DM`
- 開発ブランチ: `claude/periodic-email-delivery-system-n4yjbs`

---

## 2. 確定事項（ユーザー決定・日付順）

| 決定日 | 項目 | 決定内容 |
|---|---|---|
| 2026-08-27 | 同一相手への最短接触間隔 | **3日**（内容が違えば週2回まで）。これはシステム全体の**下限**で、キャンペーン側の設定で下回れない |
| 2026-08-27 | 配信の構造 | **1本のシリーズを進めつつ、必要な時期に別シリーズを割り込ませる**（両方を使う）。優先度つきの並行配信として実装済み |
| 2026-08-27 | 送信ドメイン | **既存ドメイン**（何年も業務で使用中） |
| 2026-08-27 | 業務メールとの分離 | **このDM専用の From アドレスを新設する**（例 `pr@` / `dm@`）。代表窓口（`info@` 等）は使わない。Gmail が用途ごとの From 分離を推奨しているため。送信IPの分離は今回は行わない |
| 2026-08-27 | メールのプレビュー | 必要に応じて提示できるよう機能に組み込む → `dm preview --html` として実装済み |
| 2026-08-27 | 1日あたりの送信上限 | **1,000通**（検証後の宛先2,454件を3日で一巡）。Gmailが要求水準を上げる5,000通/日には届かない |
| 2026-08-27 | 配信サービス（ESP）の利用 | **使わない。** Benchmark Email 等は収集リストの使用を規約で禁じており、違反は即時アカウント停止。自社SMTPから送る |
| 2026-08-27 | リスト連携 | リストは **Cowork の別セッションが Mac の共有フォルダへ書き出す**。リポジトリ経由ではない。そのフォルダを監視し、最新CSVを自動取り込みする方式にした |
| 2026-08-27 | 実行環境 | **まず Mac 上で始める**（共有フォルダを直接読めるため）。運用が固まったら別サーバへ移す。移行は `state/dm.sqlite3` を移すだけで送信履歴ごと引き継げる |

---

## 3. リストの実数（`dm import` の結果）

| | 件数 |
|---|---|
| CSV 行数 | 5,278 |
| 登録された宛先（重複統合後） | 4,568 |
| メール送信可（検証前） | 2,520 |
| **メール送信可（DNS検証後）** | **2,454**（ユニークドメイン 2,326） |
| フォーム送信可 | 3,772 |
| 除外（メール・フォームとも使用不可） | 706 |
| フリーメール判定 | 168 |
| ドメイン不一致判定 | 784 |

### DNS検証の実測結果（2026-08-27・全2,326ドメイン）

| | 件数 |
|---|---|
| 到達可 | 2,254 |
| 到達不可（送っても届かない） | 66（判定できたうちの 2.8%） |
| 判定不能（DNS到達せず・送信対象に残置） | 5 |

この66件をそのまま送っていれば、初回配信でいきなり2.8%のバウンスが出ていた。
**リストを更新したら、そのつど `dm verify` を実行すること。**

一巡に要する日数（検証後のメール2,454件）:

| 1日あたり | 一巡 |
|---|---|
| 500通 | 5日 |
| **1,000通（採用）** | **3日** |
| 2,000通 | 2日 |

---

## 4. 未決事項（次に決めるもの）

1. **シリーズの構成**（何通で、どんな内容を、何日間隔で）— コンテンツはユーザーが用意予定
2. ~~共有フォルダのパス~~ → **判明済み**（下記「リストの書き出し元」を参照）
3. `.env` の送信者情報の設定
   - **`DM_SENDER_EMAIL` はDM専用のアドレスにすること**（上記の決定事項）
   - SMTP の接続情報（ホスト・ユーザー・パスワード）

### 進行中のマイルストーン

- [ ] **M1: Mac でリストを取り込めるところまで**（手順4まで完了、手順5で共有フォルダを特定中）
- [ ] M2: `.env` 設定 → `dm doctor --dns` が全項目OK
- [ ] M3: 文面の差し替え → `dm preview --html` で確認
- [ ] M4: 少数で試験送信（`--limit 10`）
- [ ] M5: 定期実行の登録（`deploy/macos/install.sh`）

---

## 5. 進め方についてのユーザー指示（恒久ルール）

- **質問は一つずつ。** 複数を列挙しない（チャット欄へのコピペが非効率になるため）
- **参照すべき情報は右ウィンドウに表示する**（Artifact として公開する）
- **作業が終わったら次の指示を求める。** ユーザーが「今どの段階か」を見失わないようにする
- 汎用マニュアル v15 を遵守する

---

## 6. 実装済みの機能（2026-08-27 時点）

| 領域 | 内容 |
|---|---|
| リスト取り込み | 正規化・重複統合・不正データの除外（`dm import`） |
| 配信の決定 | ステップ順の進行、最短間隔、ドメイン単位の上限、配信停止の反映（`dm/selector.py`） |
| 並行配信 | 優先度つき。`--campaign all` で複数シリーズをまとめて実行 |
| メール送信 | console / .eml出力 / SMTP。List-Unsubscribe 付与 |
| フォーム送信 | Playwright。CAPTCHA は回避せず中止、robots.txt 尊重、確認画面対応、証跡保存 |
| 法令チェック | 送信直前に本文を検査し、表示が欠けていれば送らない（fail-closed） |
| 到達性チェック | SPF / DKIM / DMARC / 逆引き / From整合／DM専用アドレスかを検査（`dm doctor --dns`） |
| 送信量の制御 | 1回あたり・1時間あたり・1日あたりの上限。到達したら翌日へ繰り越し |
| バウンス処理 | IMAP から恒久・一時バウンスと配信停止の申し出を取り込み |
| プレビュー | 全ステップの文面を1ページに書き出す（`dm preview --html`） |
| リスト検証 | 宛先ドメインを DNS で実測し、届かないものを外す（`dm verify`）。判定結果は記録され、再取り込みでも失われない |
| リスト更新の安全性 | CSVから消えた宛先を検出。削除はせず `--deactivate-missing` で除外 |
| リストの自動取り込み | 監視フォルダ内の最新CSVを自動選択（`contacts_dir`）。宛先が20%超減る取り込みは書き込む前に中止 |
| 定期実行 | macOS(launchd) / Linux(systemd) / cron。多重起動防止つき |

テスト152件。

---

## 7. よく使うコマンド

```bash
python -m dm.cli doctor --dns                                   # 送信前の設定・認証チェック
python -m dm.cli verify                                         # 宛先ドメインを実測し届かないものを外す
python -m dm.cli import --deactivate-missing                    # リスト更新（消えた宛先を除外）
scripts/periodic_run.sh refresh                                 # 取り込み＋検証（配信より前に回す）
python -m dm.cli plan --campaign all --channel email            # 今回誰に何が送られるか
python -m dm.cli preview --campaign <key> --html state/preview.html  # 文面を1ページに書き出す
python -m dm.cli send --campaign all --live                     # メール配信
python -m dm.cli form --campaign all --live                     # フォーム送信
python -m dm.cli ingest --imap                                  # バウンス・配信停止の取り込み
python -m dm.cli stats / runs / campaigns                       # 状況確認
```

テスト実行（フォーム送信のテストには Chromium が必要）:

```bash
DM_CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome python -m pytest -q
```

---

## 8. 注意点・過去に踏んだ問題

- **`git push` が権限で拒否されることがある。** その場合は `git push`（引数なし）で通ることを確認済み。
- この実行環境では **UDP/53 が塞がれている**ため、DNS は HTTPS 経由（DoH）へ自動的に切り替わる。
  ユーザーのサーバでは通常の DNS が使われる。
- `state/` と `.env` は `.gitignore` 済み。送信履歴（`state/dm.sqlite3`）は**バックアップ対象**。
  これを失うと二重送信が起きる。
- `data/*.csv` には取引先候補の連絡先が入っている。リポジトリを公開する場合はリポジトリ外へ移す。
- **認証情報をチャットに貼らないこと。** 一度貼られた値は、作業完了後に変更する（マニュアル §8-4）。
  受け渡しは `.env`（権限600）または資格情報ストア経由とする。
- 収集リストには、画像ファイル名をアドレスとして拾ったもの（`...@3x.avif` 等）が混ざる。
  取り込み時に落としているが、リストの生成側でも確認するとよい。

### リストの書き出し元（2026-08-28 判明）

Cowork の別セッションは、Dropbox 内の下記フォルダへ書き出している。

```
/Users/yoshitakaikushima/JAPAN PROMOTION Dropbox/Ikushima Yoshitaka/営業/営業先開拓/outreach_starter_kit_v1/master
```

**書き出しのたびに `integration_<日時>/` という新しいサブフォルダが作られる。**
古いものは `archive_*/superseded_outputs/` へ移される。
そのため監視は再帰的でなければならず、`contacts_glob` の既定を
`**/master_contacts_*.csv` に変更した（平置きのフォルダでもそのまま動く）。

`master/` を指すこと。`outreach_starter_kit_v1/` を指すと archive 配下も対象に入る
（最新を選ぶので実害は出にくいが、対象を絞る方が安全）。

- **Dropbox 配下である点に注意。** オンラインのみ（実体が未ダウンロード）の状態だと
  読み取れない可能性がある。【未確認・推測】初回の取り込みで確認すること。

### ユーザーの Mac 環境（2026-08-27 実測）

- `git version 2.39.5 (Apple Git-154)` / `Python 3.14.7`
- リポジトリは `~/DM`、実行環境は `~/DM/.venv`（`pip install -e ".[form,dns,dev]"` は成功）
- **Python 3.14 のため、M4（フォーム送信）の前に `playwright install chromium` が
  この版で通るかを必ず確認すること。** 未確認。

### 失敗の記録

- **手順書のドラッグ＆ドロップ指示が機能しなかった（2026-08-27）**：共有フォルダの場所を
  調べる手順で「Finder からターミナルへフォルダをドラッグ」と指示したが、ユーザーの画面では
  `echo` が空行を返すだけで場所が取得できなかった。原因＝ドラッグという操作は、
  文章で説明しても再現されにくい。再発防止＝**手作業の代わりに機械に探させる**
  （`mdfind -name "master_contacts_" -onlyin ~`）。手順書は修正済み。
