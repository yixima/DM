# この案件で最初に読むもの

1. **`docs/L0_core_card_v17.md`** — 汎用マニュアル v17 コアカード（L0・139行）。
   **コマンド入力の有無にかかわらず、毎回の応答に常時適用する。**
   この環境は表の **[Code]**（Claude Code）に該当する。
2. **`docs/manual_v17_all_in_one.md`** — 同 v17 の全部入り（L0＋L1本編＋L2記録・1,098行）。
   コアカードの各項から参照して開く。**L0 に載らない条項も免除されない。**
   第1部はコアカードと同一内容（照合済み）。
3. **`docs/PROJECT_NOTES.md`** — この案件固有の確定事項・未決事項・マイルストーン。
4. **`README.md`** — システムの構成と使い方。

**v17 より前の版（v5・v13・v15・v16 等）は「保管のみ」とし、判断の根拠に用いない**
（全部入り「このファイルの扱い」3項）。旧版の全条文は v17 第2部に引き継がれている。

---

## この案件で特に効く条項

| 場面 | 条項 |
|---|---|
| ユーザーに操作を依頼する | **一度に1ステップだけ。** そのステップの完了を確認してから次を出す（コアカード §5.5） |
| 質問する | **一度に一つだけ。** 質問だけの応答で終わらない（§4） |
| 認証情報を扱う | チャットに貼らせない。`.env`（権限600）へユーザーが手元で直接入力する |
| 一括送信を行う | 送信は不可逆。§3-10／§8-5／§10-6 が自動発動する |
| ファイルを納品・共有する | ファイル名は `^[A-Za-z0-9._-]+$` のみ（§5-1） |

## この案件の技術的な地雷

- **コミットメッセージは必ずファイルに書き、`git commit -F` で渡す。**
  `printf` を使うと本文中の `%` や全角括弧が書式指定子として解釈され、そこから先が消える（実際に2回発生）。
- `git push -u origin <ブランチ>` が拒否されることがある。`git push`（引数なし）は通る。
- `state/dm.sqlite3` は送信履歴。**失うと二重送信が起きる。** バックアップ対象。
- `dm/compliance.py` のチェックは緩めない。`dm/formbot.py` に CAPTCHA 回避を実装しない。
